# Copy to .env and fill in real values. .env is gitignored — never commit it.
# Real values arrive when P0-01 wires up MySQL + Spring; placeholders for now.

# --- MySQL (docker-compose) ---
MYSQL_DATABASE=ledgerpay
MYSQL_USER=ledgerpay
MYSQL_PASSWORD=change_me
MYSQL_ROOT_PASSWORD=change_me_too

# --- Spring datasource ---
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/ledgerpay
SPRING_DATASOURCE_USERNAME=ledgerpay
SPRING_DATASOURCE_PASSWORD=change_me

# --- Stripe (test mode only — arrives in P3) ---
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
